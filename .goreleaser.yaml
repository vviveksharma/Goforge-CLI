version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: goforge
    main: ./cmd/goforge
    binary: goforge
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/viveksharma/goforge/internal/cmd.version={{.Version}}
      - -X github.com/viveksharma/goforge/internal/cmd.commit={{.Commit}}
      - -X github.com/viveksharma/goforge/internal/cmd.date={{.Date}}

archives:
  - id: default
    format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    format_overrides:
      - goos: windows
        format: zip
    files:
      - README.md
      - LICENSE
      - SECURITY.md

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'

release:
  github:
    owner: viveksharma
    name: goforge
  draft: false
  prerelease: auto
