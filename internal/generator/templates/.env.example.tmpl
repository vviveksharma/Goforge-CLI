# Application Configuration
APP_ENV=development
APP_PORT=8080

# Database Configuration
# Security: Never commit real credentials to version control
DATABASE_URL=postgres://{{.ProjectName}}_user:changeme123@localhost:5432/{{.ProjectName}}_db?sslmode=disable

# Redis Configuration
REDIS_URL=redis://localhost:6379/0

# Logging
LOG_LEVEL=info

# Security Headers (production)
# ENABLE_HSTS=true
# HSTS_MAX_AGE=31536000
